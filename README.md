# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

В приложении используются следующие сущности:

### Iproduct
Интерфейс, описывающий товар, получаемый с сервера. 

interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

### IBuyer

interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}


## Модели данных

### Класс CatalogModel

**Назначение:** Хранит массив всех товаров и товар, выбранный для подробного отображения.

**Поля класса:**
`_items: IProduct[]` - массив всех товаров
`_selectedItem: IProduct | null` - товар, выбранный для подробного отображения

**Методы:**
`setItems(items: IProduct[]): void` - сохранение массива товаров, полученного в параметрах метода
`getItems(): IProduct[]` - получение массива товаров из модели
`getItemById(id: string): IProduct | undefined` - получение одного товара по его id
`setSelectedItem(item: IProduct): void` - сохранение товара для подробного отображения
`getSelectedItem(): IProduct | null` - получение товара для подробного отображения

### Класс CartModel

**Назначение:** Хранит массив товаров, выбранных покупателем для покупки.

**Поля класса:**
`_items: IProduct[]` - массив товаров, выбранных покупателем для покупки

**Методы:**
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине
`addItem(item: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины
`removeItem(id: string): void` - удаление товара, полученного в параметре, из массива корзины
`clear(): void` - очистка корзины
`getTotal(): number` - получение стоимости всех товаров в корзине
`getCount(): number` - получение количества товаров в корзине
`hasItem(id: string): boolean` - проверка наличия товара в корзине по его id, полученному в параметре метода

### Класс OrderModel

**Назначение:** Хранит данные покупателя для оформления заказа.

**Поля класса:**
`_payment: TPayment | null` - вид оплаты
`_address: string` - адрес
`_phone: string` - телефон
`_email: string` - email

**Методы:**
`setData(data: Partial<IBuyer>): void` - сохранение данных в модели. Позволяет сохранить как все данные, так и отдельные поля (например, только адрес или только телефон)
`getData(): IBuyer` - получение всех данных покупателя
`clear(): void` - очистка данных покупателя
`validate(): Partial<Record<keyof IBuyer, string>>` - валидация данных. Возвращает объект с ошибками. Если поле валидно (не пустое), оно отсутствует в объекте. Пример: `{ payment: 'Не выбран вид оплаты', email: 'Укажите email' }`

## Слой коммуникации

### Класс LarekApi

**Назначение:** Обеспечивает взаимодействие с сервером API интернет-магазина.

**Поля класса:**
`_api: IApi` - хранит ссылку на объект API для выполнения запросов
`_baseUri: string` - хранит базовый адрес сервера

**Методы класса:**

`getProducts(): Promise<IProduct[]>`
Выполняет GET-запрос на эндпоинт `/product` для получения списка всех товаров.

`createOrder(order: IOrderRequest): Promise<IOrderResponse>`

Выполняет POST-запрос на эндпоинт `/order` для отправки данных заказа на сервер.







## Слой представления (View)

### Класс Page
**Назначение:** Управляет главными элементами страницы: каталогом товаров, счетчиком корзины и блокировкой прокрутки при открытых модальных окнах.

**Конструктор:**
* `constructor(container: HTMLElement, protected events: IEvents)` — принимает корневой элемент страницы и брокер событий.

**Поля класса:**
* `_counter: HTMLElement` — элемент счетчика товаров в корзине.
* `_catalog: HTMLElement` — контейнер для вывода карточек товаров.
* `_wrapper: HTMLElement` — обертка страницы для блокировки скролла.
* `_basket: HTMLElement` — кнопка открытия корзины.

**Методы (сеттеры):**
* `set counter(value: number)` — обновляет значение счетчика корзины.
* `set catalog(items: HTMLElement[])` — заменяет содержимое каталога на переданный массив DOM-элементов карточек.
* `set locked(value: boolean)` — переключает класс-модификатор для блокировки прокрутки страницы.

---

### Класс Modal
**Назначение:** Реализует базовое модальное окно. Управляет его открытием, закрытием и содержимым.

**Конструктор:**
* `constructor(container: HTMLElement, protected events: IEvents)` — принимает контейнер модального окна и брокер событий.

**Поля класса:**
* `_closeButton: HTMLButtonElement` — кнопка закрытия (крестик).
* `_content: HTMLElement` — контейнер для вставки содержимого (формы, карточки, корзины).

**Методы:**
* `open(): void` — открывает модальное окно и вешает слушатель на клавишу `Esc`.
* `close(): void` — закрывает окно, очищает его контент и снимает слушатель `Esc`.
* `handleEscUp(evt: KeyboardEvent): void` — обработчик закрытия по клавише `Esc`.
* `render(data: IModalData): HTMLElement` — заполняет контент и открывает окно.
* `set content(value: HTMLElement | null)` — устанавливает или очищает DOM-элемент внутри окна.

---

### Класс Card
**Назначение:** Базовый класс карточки товара. Содержит логику управления всеми возможными элементами карточки.

**Конструктор:**
* `constructor(protected blockName: string, container: HTMLElement, actions?: ICardActions)` — принимает имя BEM-блока, контейнер карточки и объект с коллбеками действий (клик).

**Поля класса:**
* `_title: HTMLElement` — элемент заголовка.
* `_price: HTMLElement` — элемент цены.
* `_image?: HTMLImageElement` — элемент картинки.
* `_category?: HTMLElement` — элемент категории.
* `_description?: HTMLElement` — элемент описания.
* `_button?: HTMLButtonElement` — кнопка действия (покупки/удаления).
* `_index?: HTMLElement` — порядковый номер (для корзины).

**Методы (сеттеры):**
* `set id(value: string)` — устанавливает `data-id`.
* `set title(value: string)` — устанавливает текст заголовка.
* `set price(value: number | null)` — устанавливает цену. Блокирует кнопку, если цена `null`.
* `set image(value: string)` — устанавливает ссылку на картинку.
* `set category(value: string)` — устанавливает текст и цвет фона категории.
* `set description(value: string)` — устанавливает текст описания.
* `set index(value: number)` — устанавливает порядковый номер.
* `set buttonTitle(value: string)` — изменяет текст на кнопке.

---

### Класс CardCatalog
**Назначение:** Дочерний класс карточки. Предназначен для отображения товара в общем каталоге на главной странице.

**Конструктор:**
* `constructor(container: HTMLElement, actions?: ICardActions)` — вызывает конструктор родителя с предустановленным `blockName = 'card'`.
*(Наследует все поля и методы родительского класса Card).*

---

### Класс CardPreview
**Назначение:** Дочерний класс карточки. Предназначен для подробного отображения товара в модальном окне.

**Конструктор:**
* `constructor(container: HTMLElement, actions?: ICardActions)` — вызывает конструктор родителя с предустановленным `blockName = 'card'`.
*(Наследует все поля и методы родительского класса Card).*

---

### Класс CardBasket
**Назначение:** Дочерний класс карточки. Предназначен для компактного отображения товара в виде строки списка внутри корзины.

**Конструктор:**
* `constructor(container: HTMLElement, actions?: ICardActions)` — вызывает конструктор родителя с предустановленным `blockName = 'card'`.
*(Наследует все поля и методы родительского класса Card).*

---

### Класс Basket
**Назначение:** Управление отображением списка выбранных товаров и итоговой стоимости в корзине.

**Конструктор:**
* `constructor(container: HTMLElement, protected events: IEvents)` — принимает DOM-элемент корзины и брокер событий.

**Поля класса:**
* `_list: HTMLElement` — контейнер для вывода элементов списка.
* `_total: HTMLElement` — элемент для вывода итоговой стоимости.
* `_button: HTMLButtonElement` — кнопка перехода к оформлению заказа («Оформить»).

**Методы (сеттеры):**
* `set items(items: HTMLElement[])` — заменяет содержимое списка. Если массив пуст, выводит текст "Корзина пуста" и блокирует кнопку «Оформить».
* `set total(total: number)` — устанавливает и выводит итоговую сумму всех товаров.

---

### Класс Form<T>
**Назначение:** Базовый класс для всех форм в приложении. Обрабатывает события ввода (`input`) и сабмита (`submit`), а также выводит ошибки валидации.

**Конструктор:**
* `constructor(protected container: HTMLFormElement, protected events: IEvents)` — принимает элемент формы и брокер событий.

**Поля класса:**
* `_submit: HTMLButtonElement` — кнопка отправки формы.
* `_errors: HTMLElement` — контейнер для вывода текста ошибок валидации.

**Методы:**
* `set valid(value: boolean)` — блокирует (disabled) или разблокирует кнопку отправки в зависимости от валидности.
* `set errors(value: string)` — устанавливает текст ошибки.
* `render(state: Partial<T> & IFormState): HTMLFormElement` — отрисовывает форму с переданным состоянием.

---

### Класс OrderForm
**Назначение:** Дочерний класс формы. Предназначен для первого шага оформления заказа (выбор способа оплаты и ввод адреса).

**Конструктор:**
* `constructor(container: HTMLFormElement, events: IEvents)` — вызывает конструктор родителя и инициализирует кнопки выбора оплаты.

**Поля класса:**
* `_card: HTMLButtonElement` — кнопка оплаты онлайн.
* `_cash: HTMLButtonElement` — кнопка оплаты при получении.

**Методы:**
* `setPayment(method: 'card' | 'cash'): void` — переключает активный класс у кнопок оплаты и вызывает событие изменения данных.
* `set address(value: string)` — устанавливает значение в поле адреса.

---

### Класс ContactsForm
**Назначение:** Дочерний класс формы. Предназначен для второго шага оформления заказа (ввод email и номера телефона).

**Конструктор:**
* `constructor(container: HTMLFormElement, events: IEvents)` — вызывает конструктор родительского класса Form.

**Методы (сеттеры):**
* `set phone(value: string)` — устанавливает значение в поле телефона.
* `set email(value: string)` — устанавливает значение в поле email.

---

### Класс Success
**Назначение:** Управляет отображением финального окна об успешном оформлении заказа.

**Конструктор:**
* `constructor(container: HTMLElement, actions: ISuccessActions)` — принимает корневой элемент сообщения и объект с коллбеком для кнопки закрытия.

**Поля класса:**
* `_close: HTMLButtonElement` — кнопка возврата к покупкам.
* `_description: HTMLElement` — элемент для вывода информации о списанной сумме.

**Методы (сеттеры):**
* `set total(value: number)` — формирует и выводит строку "Списано [сумма] синапсов".

---

## Описание событий (Presenter)

Логика взаимодействия между слоями построена на обработке следующих событий через брокер `EventEmitter`:

**События изменения данных (Model -> View):**
* `items:changed` — массив товаров каталога обновился (вызывает рендер `CardCatalog` на главной странице).
* `preview:changed` — изменился выбранный товар (открывает модалку с `CardPreview`).
* `basket:changed` — изменился состав корзины (обновляет счетчик в `Page` и перерисовывает список `CardBasket` внутри `Basket`).
* `formErrors:change` — изменились ошибки валидации (передает ошибки в формы и переключает состояние кнопок сабмита).

**События взаимодействия с интерфейсом (View -> Presenter):**
* `card:select` — клик по карточке в каталоге (вызывает метод модели для установки товара в превью).
* `basket:open` — клик по иконке корзины (рендерит компонент `Basket` в модальное окно).
* `order:open` — клик по кнопке «Оформить» в корзине (рендерит `OrderForm` в модальное окно).
* `/^(order\..*):change/` — изменение полей ввода в формах (записывает обновленные данные в модель `OrderModel`).
* `order:submit` — успешный ввод адреса и выбор оплаты (открывает второй шаг — `ContactsForm`).
* `contacts:submit` — отправка формы контактов (собирает данные из `OrderModel` и `CartModel`, делает POST-запрос через API, при успехе показывает компонент `Success` и очищает корзину).
* `modal:open` / `modal:close` — события изменения состояния модального окна (блокируют/разблокируют скролл страницы через компонент `Page`).